# Digit_Recognition
This project is a Handwritten Digit Recognition System developed using Deep Learning with TensorFlow and Keras. The model is trained on the MNIST dataset to recognize handwritten digits (0–9). A Streamlit web application is used to provide a simple interface where users can upload a handwritten digit image and receive an instant prediction.
